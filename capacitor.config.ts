import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fleet.proyecto.movil',
  appName: 'Proyecto Movil',
  webDir: 'dist',
};

export default config;